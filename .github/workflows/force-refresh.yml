name: timed-redeploy
on:
  schedule:
    - cron: "*/15 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel API
        run: |
          curl --request GET \
          --url '${{ secrets.FORCE_REFRESH }}' \
