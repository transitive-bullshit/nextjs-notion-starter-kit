name: Deploy
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0/5 * * *"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        env:
          DEPLOY_URL: ${{ DEPLOY_URL }}
        run: curl -X POST $DEPLOY_URL
